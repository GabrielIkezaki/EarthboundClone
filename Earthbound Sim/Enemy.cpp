#include "Enemy.h"

#pragma region Previous Code
/*
//This is where every trait will be inserted in the actual sprite
void Enemy::DefineSprite() {


	enemySprite.setTexture(enemyTexture);
	enemySprite.setPosition(xPos, yPos);
	enemySprite.setScale(xScale, yScale);
}

//This is where we define the Set function, where the encapsulation takes place
void Enemy::Set(std::string tempTexture, float tempXPos, float tempYPos, float tempXScale, float tempYScale) {
	enemySpritePntr = &enemySprite;
	textureAddress = tempTexture;
	xPos = tempXPos;
	yPos = tempYPos;
	xScale = tempXScale;
	yScale = tempYScale;

	enemyTexture.loadFromFile(textureAddress);

	DefineSprite();
}*/

#pragma endregion




